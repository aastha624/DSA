#include<stdio.h>
struct myArray  {
    int totalsize;
    int usedsize; 
    int *ptr;//primitive data type
};
void createArray(struct myArray*a)//abstract data type-coustem data type
int main(){
    struct myArray marks;

    return 0; 
}